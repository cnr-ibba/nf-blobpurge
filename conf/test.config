/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    All test data under assets/test/ is synthetic (see
    assets/test/generate_test_data.py): a 6-contig toy assembly with one
    intentionally near-duplicate ("heterozygous haplotig") contig pair and
    two "contaminant" contigs, plus a matching hand-built reads CRAM and a
    hand-built BlobDir. It is a plumbing fixture, not biologically real data.

    Use as follows:
        nextflow run cnr-ibba/nf-blobpurge -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '8.GB',
        time: '1.h'
    ]
}

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal synthetic dataset to check pipeline function end-to-end'

    // nf-schema resolves relative paths in a samplesheet against the launch
    // directory, not the CSV's own location, so the bundled local test data
    // needs a samplesheet with absolute paths. It is generated here, at
    // config-load time, from ${projectDir} so it stays portable across
    // checkouts. Strict config syntax forbids top-level `def` statements
    // mixed with config blocks, so generation is wrapped in a closure that
    // is immediately invoked via `.call()`.
    input = {
        def blobpurge_test_samplesheet = new File("${System.getProperty('java.io.tmpdir')}/blobpurge_test_samplesheet_${System.getProperty('user.name')}.csv")
        blobpurge_test_samplesheet.text = [
            'sample_id,assembly,blobdir,reads_cram',
            [
                'blobpurge_test',
                "${projectDir}/assets/test/assembly.fasta",
                "${projectDir}/assets/test/blobdir",
                "${projectDir}/assets/test/reads.cram",
            ].join(',')
        ].join('\n') + '\n'
        return blobpurge_test_samplesheet.toString()
    }.call()

    // BTK_FILTER: --taxrule is deliberately left unset here to also exercise
    // the meta.json-based auto-detection path (see bin/detect_taxrule.py).
    exclude_taxa = 'Pseudomonadota,Bacteroidota'
    taxon_field  = 'buscoregions_phylum'

    // A tiny, fast-to-download lineage is used purely to keep CI runtime
    // down; it is not biologically appropriate for the synthetic contigs.
    busco_lineages = 'bacteria_odb10'

    // Deliberately tiny so the 6-contig fixture (raw span 28,200bp) is
    // forced into several real chunks, exercising the split -> multiple
    // BUSCO tasks -> merge plumbing end-to-end rather than the trivial
    // single-chunk passthrough. In fixture record order (ctg1..ctg6,
    // 3000/3000/12000/1200/1000/8000bp), sequential accumulation with this
    // threshold yields 5 raw-stage chunks and, importantly, lands the
    // fixture's intentional near-duplicate pair (ctg1/ctg2) in *different*
    // chunks.
    busco_chunk_max_span = 3000

    run_purge_haplotigs = true
}
